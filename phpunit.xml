<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>

        <!--
            The AI provider is PINNED here, not inherited from .env.

            The client tests fake HTTP against whichever base URL the active
            provider uses. Left to .env, changing AI_PROVIDER on a laptop sent
            those requests straight past the fake to the real API — with the
            real key, spending real credits and shipping the test prompts to a
            provider. That happened once, on 2026-08-13, switching to
            OpenRouter. Tests must not depend on developer configuration.

            The keys are blanked for the same reason: belt as well as braces,
            so a stray request cannot authenticate even if one escapes.
            Http::preventStrayRequests() in tests/Pest.php is the braces.
        -->
        <env name="AI_PROVIDER" value="deepseek"/>
        <env name="OPENROUTER_API_KEY" value=""/>
        <env name="OPENROUTER_MANAGEMENT_KEY" value=""/>

        <!--
            Pinned for the same reason: a test that asserts on a rendered date
            reads the month name through the locale, and .env.example still
            ships APP_LOCALE=en. Left inherited, those assertions pass on this
            laptop and fail on a fresh clone.

            The timezone is NOT pinned here, deliberately — it is hardcoded in
            config/app.php rather than read from env, so it is already the same
            everywhere and there is nothing for a machine to disagree about.
        -->
        <env name="APP_LOCALE" value="es"/>
    </php>
</phpunit>
